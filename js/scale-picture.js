// const decreaseButton = document.querySelector('.scale__control--smaller');
// const increaseButton = document.querySelector('.scale__control--bigger');
// const scaleValue = document.querySelector('.scale__control--value');
// const currentPicture = document.querySelector('.img-upload__preview').querySelector('img');
// const MAX_VALUE_COUNT = 100;

// const scalePicture = () => {
//   scaleValue.value = MAX_VALUE_COUNT;
//   let valueCount = MAX_VALUE_COUNT;
//   if (scaleValue.value === 25 || scaleValue.value === 100) {
//     decreaseButton.disabled = true;
//   } else {
//     decreaseButton.addEventListener('click', () => {
//       currentPicture.style.transform = `scale(0.${valueCount -= 25})`;
//     });
//     increaseButton.addEventListener('click', () => {
//       currentPicture.style.transform = `scale(0.${valueCount += 25})`;
//     });
//   }
// };

// export {scalePicture};
